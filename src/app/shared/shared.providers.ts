import { Provider } from '@angular/core';

/**
 * Shared providers for the application.
 * Note: Use this for global features like custom icons, localizations, etc.
 */
export const provideShared = (): Provider[] => {
  return [];
};
